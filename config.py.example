"""Configuration settings for the extractor - COPY THIS TO config.py AND ADD YOUR API KEY."""

import os
from openai import OpenAI

# API Configuration
# Cloudflare Workers AI free tier (OpenAI-compatible, hosts
# @cf/meta/llama-3.1-8b-instruct-fp8) -- get a free API token at
# https://dash.cloudflare.com/profile/api-tokens (needs Workers AI permission)
# and your account ID from the dashboard sidebar.
API_BASE_URL = "https://api.cloudflare.com/client/v4/accounts/YOUR_ACCOUNT_ID_HERE/ai/v1"
# IMPORTANT: Replace with your actual API key
API_KEY = "YOUR_API_KEY_HERE"
DEFAULT_MODEL = "@cf/meta/llama-3.1-8b-instruct-fp8"

# Processing Configuration
SAFE_DELAY = 1
DEFAULT_TEMPERATURE = 0.1

# Page Extraction Settings
METADATA_PAGES = [0]
INDICATIONS_PAGES = [1, 3]
WARNINGS_PAGES = [3, 5]
TOC_SEARCH_PAGES = 3
DEFAULT_ADVERSE_START_PAGE = 9
DEFAULT_ADVERSE_END_PAGE_OFFSET = 5

# Output folder names
INDICATIONS_OUTPUT_FOLDER = "indications"
CONTRAINDICATIONS_OUTPUT_FOLDER = "contraindications"
WARNINGS_OUTPUT_FOLDER = "warnings"
ADVERSE_EVENTS_OUTPUT_FOLDER = "adverse_events"
DRUG_INTERACTIONS_OUTPUT_FOLDER = "drug_interactions"
LIVER_OUTPUT_FOLDER = "liver"
KIDNEY_OUTPUT_FOLDER = "kidney"
PK_OUTPUT_FOLDER = "pharmacokinetics_summaries"
METABOLISM_OUTPUT_FOLDER = "metabolism"
ELIMINATION_OUTPUT_FOLDER = "elimination"
PD_OUTPUT_FOLDER = "pharmacodynamics_summaries"
PREGNANCY_OUTPUT_FOLDER = "pregnancy_summaries"
BREASTFEEDING_OUTPUT_FOLDER = "breastfeeding_summaries"

# Output Settings
OUTPUT_COLUMN_ORDER = [
    "ID", 
    "Brand Name", 
    "Company", 
    "Initial Authorization",
    "Revision Date", 
    "Ingredients", 
    "Dosage",
    "Indications",
    "Contraindications",
    "Serious Warnings",
    "Adverse Events", 
    "Drug Interactions",
    "Liver Dose Adjustment",
    "Kidney Dose Adjustment",
    "Pharmacokinetics Summary",
    "Metabolism",
    "Elimination",
    "Pharmacodynamics",
    "Pregnancy Recommendation",
    "Pregnancy Summary",
    "Breastfeeding Recommendation",
    "Breastfeeding Summary"
]

# Initialize OpenAI client (will be replaced with actual key)
client = OpenAI(
    base_url=API_BASE_URL,
    api_key=API_KEY
)